#include "menu.h"

int main()
{
    dynamic();//оголошення тривимірного масиву та виділення для нього пам'яті
    main_menu();//головне меню
    free_mem();//звільнення пам'яті

    //Для перевірки якості сортування:
    //out();//функція виведення тримірного масиву до та після сортування
    //v_out();//функція виведення вектору до та після сортування
    //(алгоритм обирається в самій функції у модулі filling.c)
    return 0;
}

